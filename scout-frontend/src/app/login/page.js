// This is the route file for /login
import LoginPage from '../LoginPage'; // Adjust path if your LoginPage component is elsewhere

export default function LoginRoutePage() {
  return <LoginPage />;
}
